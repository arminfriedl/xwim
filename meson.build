project('xwim', 'cpp',
        version: '0.3',
        default_options: ['cpp_std=c++17',
                          'warning_level=3',
                          'b_coverage=true',
                          'b_ndebug=if-release'])

subdir('src')
subdir('doc')
subdir('test')
